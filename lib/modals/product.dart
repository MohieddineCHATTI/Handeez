class Product {
  String pName, pPrice, pLocation, pDescription, pCategory, pId;
  int quantity;
  Product(
      {this.quantity,
      this.pName,
      this.pPrice,
      this.pCategory,
      this.pDescription,
      this.pLocation,
      this.pId});
}
